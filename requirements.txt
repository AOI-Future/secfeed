# FastMCP 4 is the first line with native MCP 2026-07-28 support. Keep the
# production image on the pre-v4 protocol until a v4 integration canary passes.
fastmcp>=2.0.0,<4.0.0
httpx>=0.27.0
feedparser>=6.0.0
